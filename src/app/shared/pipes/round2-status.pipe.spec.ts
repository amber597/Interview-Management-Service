import { Round2StatusPipe } from './round2-status.pipe';

describe('Round2StatusPipe', () => {
  it('create an instance', () => {
    const pipe = new Round2StatusPipe();
    expect(pipe).toBeTruthy();
  });
});
