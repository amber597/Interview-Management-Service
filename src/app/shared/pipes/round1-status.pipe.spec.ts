import { Round1StatusPipe } from './round1-status.pipe';

describe('Round1StatusPipe', () => {
  it('create an instance', () => {
    const pipe = new Round1StatusPipe();
    expect(pipe).toBeTruthy();
  });
});
